import React from 'react';

const BigOutlineBtn = ({title}) => {
    return (
        <button className='border-2 border-white px-8 py-2 rounded-full uppercase text-white'>{title}</button>
    );
};

export default BigOutlineBtn;